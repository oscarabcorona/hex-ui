/**
 * Shared UI class strings for the docs surface. Single source of truth —
 * any chrome that reads "the article body max-width" imports from here.
 */

/** Article-body max-width wrapper. Caps measure at ~65ch on ultra-wide. */
export const DOCS_CONTENT_WRAPPER = "mx-auto max-w-3xl xl:max-w-4xl";
