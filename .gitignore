node_modules/
dist/
.tsup/
.turbo/
*.tsbuildinfo
# CLI prebuild copies repo-root skills/ here. Source of truth is ./skills at repo root.
packages/cli/skills/
# CLI + payload prebuild copies repo-root registry/ here. Source of truth is ./registry at repo root.
# (mcp-server no longer bundles its own copy as of the @hex-core/payload extraction.)
packages/cli/registry/
packages/payload/registry/
.DS_Store
.env
.env.local
.npmrc.publish-local
.cache/
.claude/
.idea/
.playwright-mcp/
.qa/
test-results/
playwright-report/
.playwright/
a11y-report.json
a11y-report.md
CLAUDE.md
apps/**/CLAUDE.md
*.png
# Visual regression baselines must be committed — they ARE the source of
# truth that CI diffs against. Override the broad *.png rule above.
!apps/docs/e2e/visual.spec.ts-snapshots/*.png

# Local UI reference material — structural inspiration only, not part of the
# project source. Re-authored blocks live in packages/components; these
# exemplars must not be committed.
references/

# Temporary feature POCs generated by `hex poc` — not part of the repo
crm-poc*/

# Expo (native playground)
.expo/
.expo-export/
