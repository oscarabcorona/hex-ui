{
  "$schema": "https://hex-core.dev/schema/registry-item.json",
  "name": "native-separator",
  "displayName": "Separator",
  "description": "A visual divider between content sections with horizontal or vertical orientation.",
  "category": "primitive",
  "subcategory": "layout",
  "version": "0.1.0",
  "framework": "react",
  "platform": "native",
  "props": [
    {
      "name": "orientation",
      "type": "enum",
      "required": false,
      "default": "horizontal",
      "description": "Direction of the separator",
      "enumValues": [
        "horizontal",
        "vertical"
      ]
    },
    {
      "name": "decorative",
      "type": "boolean",
      "required": false,
      "default": true,
      "description": "If true, separator is purely visual and hidden from screen readers"
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Additional NativeWind classes, merged with the component's own so a conflicting utility wins"
    }
  ],
  "variants": [],
  "slots": [],
  "files": [
    {
      "path": "components/ui/separator.tsx",
      "content": "import * as SeparatorPrimitive from \"@rn-primitives/separator\";\nimport type { ComponentProps } from \"react\";\nimport { cn } from \"../../lib/utils.js\";\n\n/** Props for {@link Separator}. */\nexport type SeparatorProps = ComponentProps<typeof SeparatorPrimitive.Root>;\n\n/**\n * A one-pixel rule between content groups.\n *\n * Decorative by default, which hides it from assistive tech. Pass\n * `decorative={false}` when the rule is the only thing marking a boundary\n * that matters — a thematic break between two unrelated sections.\n * @param props - {@link SeparatorProps}\n * @returns The separator element\n * @example\n * ```tsx\n * <Separator className=\"my-4\" />\n * <Separator orientation=\"vertical\" className=\"mx-2 h-4\" />\n * ```\n */\nexport function Separator({\n\tclassName,\n\torientation = \"horizontal\",\n\tdecorative = true,\n\t...props\n}: SeparatorProps) {\n\treturn (\n\t\t<SeparatorPrimitive.Root\n\t\t\tdecorative={decorative}\n\t\t\torientation={orientation}\n\t\t\tclassName={cn(\n\t\t\t\t\"shrink-0 bg-border\",\n\t\t\t\torientation === \"horizontal\" ? \"h-px w-full\" : \"h-full w-px\",\n\t\t\t\tclassName,\n\t\t\t)}\n\t\t\t{...props}\n\t\t/>\n\t);\n}\n",
      "type": "component"
    },
    {
      "path": "lib/animated.ts",
      "content": "import { cssInterop } from \"nativewind\";\nimport { Animated } from \"react-native\";\n\n/**\n * `Animated.View`, registered so `className` actually applies.\n *\n * NativeWind registers a fixed list of React Native components with\n * `cssInterop` — Image, Pressable, Text, View, ScrollView, TextInput and a\n * handful more — and `Animated.View` is not among them. A `className` on the\n * bare `Animated.View` is therefore an inert prop: no error, no style, just a\n * component that renders unstyled unless the consumer's Metro happens to run\n * `nativewind/babel` over `node_modules`, which this package's build\n * deliberately does not assume.\n *\n * Registering once here, in a module every animated component imports, keeps\n * that assumption out of the consumer's build config. Components that animate\n * (Progress, Skeleton, BottomSheet) must use this rather than\n * `Animated.View` directly.\n */\nexport const AnimatedView = Animated.View;\n\ncssInterop(AnimatedView, { className: \"style\" });\n",
      "type": "lib"
    },
    {
      "path": "lib/text-context.tsx",
      "content": "import { createContext, useContext } from \"react\";\n\n/**\n * Class names a parent wants applied to the `<Text>` elements inside it.\n *\n * React Native text does not inherit colour or size from its container the\n * way DOM text does, so a `Button` cannot make its label\n * `text-primary-foreground` by styling itself. Instead it provides that\n * class here and `Text` reads it. Same pattern React Native Reusables\n * uses; kept as a tiny shared module so every component agrees on it.\n */\nexport const TextClassContext = createContext<string | undefined>(undefined);\n\n/**\n * Read the class names the nearest text-styling ancestor provided.\n * @returns The inherited class string, or undefined outside any provider\n */\nexport function useTextClass(): string | undefined {\n\treturn useContext(TextClassContext);\n}\n",
      "type": "lib"
    },
    {
      "path": "lib/utils.ts",
      "content": "import { clsx, type ClassValue } from \"clsx\";\nimport { twMerge } from \"tailwind-merge\";\n\n/**\n * Merge class names with Tailwind-aware conflict resolution.\n *\n * Deliberately a copy of the web helper rather than an import from\n * `@hex-core/components`: that package is React DOM, and `hex add` ships\n * this file into the consumer's project alongside each component anyway.\n * NativeWind's platform prefixes (`ios:`, `android:`, `native:`) are\n * unknown to tailwind-merge and pass through untouched.\n * @param inputs - Class values (strings, arrays, conditionals) to merge\n * @returns The merged class string with later Tailwind utilities winning\n */\nexport function cn(...inputs: ClassValue[]): string {\n\treturn twMerge(clsx(inputs));\n}\n",
      "type": "lib"
    }
  ],
  "dependencies": {
    "npm": [
      "@rn-primitives/separator",
      "clsx",
      "tailwind-merge"
    ],
    "internal": [
      "lib/utils"
    ],
    "peer": [
      "react",
      "react-native",
      "nativewind"
    ]
  },
  "tokensUsed": [
    "border"
  ],
  "examples": [
    {
      "title": "Between sections",
      "description": "A horizontal rule with vertical margin",
      "code": "<View>\n  <Text variant=\"h3\">Profile</Text>\n  <Separator className=\"my-4\" />\n  <Text>Account details</Text>\n</View>",
      "composition": [
        "layout"
      ]
    },
    {
      "title": "Vertical divider in a row",
      "description": "Needs an explicit height — a vertical rule has no intrinsic size in flexbox",
      "code": "<View className=\"flex-row items-center gap-2\">\n  <Text variant=\"small\">Draft</Text>\n  <Separator orientation=\"vertical\" className=\"h-4\" />\n  <Text variant=\"small\">Edited 2h ago</Text>\n</View>",
      "composition": [
        "metadata-row"
      ]
    },
    {
      "title": "Semantic break",
      "description": "When the rule itself marks a boundary worth announcing",
      "code": "<Separator decorative={false} className=\"my-6\" />",
      "composition": [
        "layout",
        "accessibility"
      ]
    }
  ],
  "ai": {
    "whenToUse": "Use to divide content groups: sections of a screen, groups in a settings list, items in a metadata row. Keep it decorative unless the rule is the only marker of a meaningful boundary.",
    "whenNotToUse": "Don't use it for spacing — use gap or margin classes on the container. Don't put one between every row of a list; the row backgrounds already do that work and the rules turn into visual noise.",
    "commonMistakes": [
      "Using a vertical separator without a height class — it collapses to nothing, because a 1pt-wide View in a flex row has no intrinsic height",
      "Reaching for a Separator to create space between elements — a rule is a visual statement, whereas gap-4 on the parent View is the actual fix",
      "Leaving decorative=true on a rule that marks a real thematic break, so screen-reader users lose the boundary entirely",
      "Putting a separator after the last item in a list, leaving a trailing rule against the container edge"
    ],
    "relatedComponents": [
      "native-card",
      "native-text"
    ],
    "accessibilityNotes": "Decorative by default, so assistive tech skips it and the content around it reads continuously. Pass decorative={false} to expose it as a separator element when the boundary is semantic. It draws in the border token, which is contrast-audited in light and dark, but never rely on the rule alone to convey grouping — a heading does that job for screen-reader users.",
    "tokenBudget": 272
  },
  "tags": [
    "separator",
    "divider",
    "hr",
    "layout",
    "native",
    "react-native"
  ]
}